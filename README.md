# termux-vscode-reh

clone repo:

```
git clone --recursive https://github.com/btwiuse/termux-vscode-reh && cd termux-vscode-reh
```

in termux:

```
make build
```

start vscode:

```
make start
```
